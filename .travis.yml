language: java
sudo: false

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.gradle

matrix:
  fast_finish: true

env:
 - TERM=dumb

script:
  - JAVA_OPTS="-Xmx384m -noverify" ./gradlew build

after_success:
  - ./gradlew coveralls